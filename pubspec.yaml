name: audio_sentiment_app
description: An audio sentimental analyzer.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flex_color_scheme: ^7.2.0
  google_fonts: ^5.1.0
  intl: ^0.18.1
  stop_watch_timer: ^3.0.1
  go_router: ^10.0.0
  dio: ^5.3.2
  speech_to_text: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true